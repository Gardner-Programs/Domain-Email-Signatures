# Path to your Google service account key JSON file.
# The key must belong to a service account with domain-wide delegation enabled.
KEY_PATH=/path/to/Keys/service-account-key.json

# The admin Google Workspace email used for API delegation.
# This account must have the Admin SDK scopes granted to the service account.
ADMIN_EMAIL=admin@yourdomain.com
